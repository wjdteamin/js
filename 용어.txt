1. <script src=""> - 외부에서 가져오는 것, 
1-1. <script> - 내가 작성한 것
2. CDN
3. bootstrap3와 4는 jQuery에 의존(dependency)한다.
4. 자바스크립트는 웹브라우저가 실행한다. 
    따라서 브라우저마다 다르게 동작할 수 있다. -> jQuery -> 호환성을 생각할 필요가 없다. 

5. type - 타입은 값에 따라 js가 판단(동적타입)
    undefined - 알 수없는 타입(기본타입)
    number
    string
    boolean
    object

동적과 정적 
동적 - 바뀐다.
정적 - 바꿀 수 없다.

6. undefined : 알 수 없는 값 타입
    null : 참조변수가 아직 가리키는 대상이 없다. (typeof로 찍으면 object가 나온다.)